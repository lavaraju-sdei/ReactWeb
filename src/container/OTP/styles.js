import {colors, Fonts} from '../../constants';
import {fontScale} from '../../helpers';

export default {
  container: {},
  acVerific: {fontWeight: 'bold', fontSize: fontScale(20)},
  messageText: {fontSize: fontScale(16)},
  buttonText: {color: colors.White},
  resend: {color: colors.lightBlue3},
};
